# Local runtimes and installers
envs/
miniconda/
Miniconda3-*.exe

# Model payloads and TurboQuant binaries
models/
turboquant-plus-*/
turboquant-plus-*.zip

# Upstream reference clone; keep this workspace repo focused on our benchmark harness.
turboquant_plus_repo/

# Runtime logs generated by llama-server at the workspace root
turboquant-server*.log

# Benchmark-generated vector indexes
bench_embeddings_turbo/indexes/

# Python/editor caches
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local virtual environments inside copied projects
.venv/
venv/

# OS/editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# === Added 2026-05-25 (Session D prep) ===

# Large model files (live in ~/models/, never in repo)
*.gguf
*.safetensors
*.bin
*.onnx

# Local agent state
.claude/

# Weekend ephemeral artifacts (regenerable; don't commit)
**/run_logs/
**/raw/*.json

# Docker artifact snapshots — Docker stack moved to beellama fork
docs/deployment/docker/artifacts/
docs/deployment/docker/_artifacts/

# Local env / secrets
.env
.env.local
*.local
.envrc

# Temporary
TODO.local.md
NOTES.local.md
